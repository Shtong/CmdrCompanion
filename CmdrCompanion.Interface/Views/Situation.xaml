<UserControl x:Class="CmdrCompanion.Interface.Views.Situation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding Situation, Source={StaticResource Locator}}">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock>Current Position: </TextBlock>
            <ContentControl Content="{Binding CurrentSituation.CurrentLocation, TargetNullValue=UNKNOWN}" />
        </StackPanel>
    </Grid>
</UserControl>
